{
  "extends": "./tsconfig.json",
  "//": "codegen.ts is tooling, not part of the service. Compiling it would move the inferred rootDir to the project root and emit everything to dist/src.",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "codegen.ts"]
}
