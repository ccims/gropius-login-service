package gropius.util.schema

/**
 * Fields in JSON Type Definition schemas.
 */
enum class SchemaField {
    REF,
    TYPE,
    ENUM,
    ELEMENTS,
    PROPERTIES,
    OPTIONAL_PROPERTIES,
    VALUES,
    DISCRIMINATOR,
    MAPPING
}